<% var classes = typeof classList !== 'undefined' ? classList : '' %>


<% if (post.tags && post.tags.length){ %>
  <%- list_tags(post.tags, {
    show_count: false,
    class: classes + ' tag o',
    style: 'none',
    separator: ''
  }) %>
<% } %>
